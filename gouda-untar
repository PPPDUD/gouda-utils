#!/usr/bin/bash
if [[ $# -lt 2 ]] ; then
    echo 'Usage: gouda-untar <FILE> <NAME>' >&2;
    exit 64
fi
gouda-chkroot
source_path=$(realpath $1)
cd /opt/gouda-packages/
mkdir -p "$2"
tar -xf "$source_path" -C "$2"
cd "$2"
./install.sh
